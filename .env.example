# Configuration Django
SECRET_KEY=votre-cle-secrete-django-super-longue-et-complexe
DEBUG=False

# Hôtes autorisés (séparés par des virgules)
ALLOWED_HOSTS=127.0.0.1,localhost,votre-app.run.app

# Base de données (optionnel - utiliser Cloud SQL PostgreSQL)
# Format: postgresql://USER:PASSWORD@//cloudsql/PROJECT_ID:REGION:INSTANCE_NAME/DATABASE_NAME
# DATABASE_URL=postgresql://user:password@//cloudsql/project-id:region:instance-name/dbname

# Configuration Cloud Run (sera automatiquement défini par Cloud Run)
# CLOUD_RUN_SERVICE_URL=https://votre-app.run.app

# Port (Cloud Run utilise 8080 par défaut)
PORT=8080
