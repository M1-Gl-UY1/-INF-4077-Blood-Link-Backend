# Exemple de fichier .env.yaml pour Cloud Run
# Copiez ce fichier en .env.yaml et remplissez avec vos vraies valeurs

SECRET_KEY: "votre-cle-secrete-django-super-longue-et-complexe-generee"
DEBUG: "False"
ALLOWED_HOSTS: "*.run.app,127.0.0.1,localhost"

# Optionnel: Configuration de la base de données Cloud SQL PostgreSQL
# Décommentez et configurez si vous utilisez Cloud SQL
# DATABASE_URL: "postgresql://USER:PASSWORD@//cloudsql/PROJECT_ID:REGION:INSTANCE_NAME/DATABASE_NAME"

# Le port est automatiquement défini à 8080 par Cloud Run
# PORT: "8080"
